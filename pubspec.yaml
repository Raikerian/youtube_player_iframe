name: youtube_player_iframe
description: Flutter port of the official YouTube iFrame player API. Supports web & mobile platforms.
publish_to: 'none'
version: 1.1.0+1

environment:
  sdk: '>=2.7.0 <3.0.0'
  flutter: '>=1.22.0 <2.0.0'

dependencies:
  flutter:
    sdk: flutter

  flutter_inappwebview: '>=4.0.0 <5.0.0'
  url_launcher: '>=5.7.10 <6.0.0'

dev_dependencies:
  flutter_test:
    sdk: flutter
